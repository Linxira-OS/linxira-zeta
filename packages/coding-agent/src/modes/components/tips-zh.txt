不想再打"继续"？发一个 "." 就行
用 /btw 问一个旁路问题
用 /tan 把当前对话分叉给后台 agent
Ctrl+D 可以退出，而且会保存你的草稿！
用 `zeta stats` 看看你最爱"压榨"哪个模型
试试任务隔离，创建 CoW worktree
需要便宜的嵌套模型调用？用 `completion(x...)`。有一大批任务？让 clanker 用起来！
代码乱成一团？试试 /omfg 抱怨一下
你知道吗？每个 kitty/tmux/cmux/zellij/wezterm 分屏都有独立会话——`zeta -c` 会恢复正确的那个
在消息里加上 `ultrathink`，开启更难的多步推理——输入时会闪现彩虹
在消息里说 `orchestrate`，让并行子 agent 驱动多阶段任务——输入时会发光
在消息里说 `workflowz`，在 eval 中用并行子 agent 驱动任务——输入时会发光
同一个 provider 登录多个账号——再次 `/login`——zeta 会在它们之间自动负载均衡
运行 `zeta auth-broker serve` 一次，所有机器即可在线拉取 token——刷新密钥永不离开主机；`zeta auth-gateway` 作为即插即用的代理，任何 OpenAI 兼容客户端都能用
按 alt+p（或 /switch）切换 provider，按 ctrl+p 循环切换角色模型（smol -> slow 等）
按 ctrl+r 搜索你的提示历史，复用过去的消息
`/force read` 在模型总选错工具时，把下一轮固定到指定工具
`/copy code` 把最后一个代码块复制到剪贴板——`/copy cmd` 复制最后的 shell/python 命令
`/shake` 把沉重的工具结果从上下文里剥离出来回收 token，无需完整 /compact——`/shake images` 只丢弃图片
实时结对：`/collab` 通过端到端加密的中继链接共享你的会话——队友运行 `/join <link>` 即可观看工具调用流，并从他们自己的 zeta 向 agent 提问
按 ← ← 深入查看运行中或已完成的 agent，检查其工具调用与记录
遇到 Codex 限流？`/usage reset` 花掉一次保存的重置额度，立即恢复配额
没有原生 tool_calling？推理 provider 解析不好？`PI_DIALECT=glm|kimi|anthropic…` 会在本地为它们处理！
打开 `/advisor` 挂载第二个模型，审查每一轮并悄悄注入建议
试试用 -> 开头写一个列表（1. 做 X，2. 做 Y）
按 shift+tab 循环切换推理努力级别
