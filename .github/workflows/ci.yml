name: CI

on:
   push:
      branches: [main]
      paths:
         - "packages/**"
         - "desktop/**"
         - "web-ui/**"
         - "crates/**"
         - "editor/**"
         - "plugins/**"
         - "scripts/**"
         - "bazel/**"
         - "MODULE.bazel"
         - "MODULE.bazel.lock"
         - "BUILD.bazel"
         - ".bazelrc"
         - ".bazelignore"
         - ".bazelversion"
         - "Cargo.toml"
         - "Cargo.lock"
         - "deny.toml"
         - "about.toml"
         - "LICENSE"
         - "THIRD-PARTY-NOTICES.txt"
         - "rust-toolchain.toml"
         - "rustfmt.toml"
         # A root dependency change (lockfile/catalog/patches) affects every
         # job; without these a lockfile-only push shipped untested — and a
         # release retagged onto such a commit never triggered its run.
         - "bun.lock"
         - "bunfig.toml"
         - "package.json"
         - "patches/**"
         - ".github/**"
   pull_request:
      branches: [main, omp2]
      paths:
         - "packages/**"
         - "desktop/**"
         - "web-ui/**"
         - "crates/**"
         - "editor/**"
         - "plugins/**"
         - "scripts/**"
         - "bazel/**"
         - "MODULE.bazel"
         - "MODULE.bazel.lock"
         - "BUILD.bazel"
         - ".bazelrc"
         - ".bazelignore"
         - ".bazelversion"
         - "Cargo.toml"
         - "Cargo.lock"
         - "deny.toml"
         - "about.toml"
         - "LICENSE"
         - "THIRD-PARTY-NOTICES.txt"
         - "rust-toolchain.toml"
         - "rustfmt.toml"
         - "bun.lock"
         - "bunfig.toml"
         - "package.json"
         - "patches/**"
         - ".github/**"
   workflow_dispatch:
      inputs:
         skip_npm:
            description: "Skip npm publish"
            type: boolean
            default: false

# Release runs publish a `v*` tag pushed atomically with main HEAD; sharing
# the cheap branch-wide `CI-refs/heads/main` group meant a later main push
# silently cancelled the in-flight release and left the tag without a GitHub
# Release or npm publish (#2564). Detect release runs at workflow-scheduling
# time via the release-script commit subject (`chore: bump version to vX.Y.Z`),
# via `v*` tag-ref dispatches, and via manual dispatches whose tag-on-HEAD
# status is only known after checkout; scope them to a per-sha group with no
# cancellation. Every other event keeps branch-wide cancellation for PR/main churn.
concurrency:
   group: "${{ github.workflow }}-${{ (startsWith(github.event.head_commit.message, 'chore: bump version to ') || startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch') && format('release-{0}', github.sha) || github.ref }}"
   cancel-in-progress: "${{ !(startsWith(github.event.head_commit.message, 'chore: bump version to ') || startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch') }}"

env:
   FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
   contents: read

jobs:
   # scripts/release.ts pushes the version-bump commit and its `v*` tag
   # atomically (`git push --atomic origin refs/heads/main:refs/heads/main
   # <sha>:refs/tags/v<version>`), so a release now arrives as a single `push` to
   # `refs/heads/main` — we no longer trigger on the tag ref at all (see
   # `on.push`). This one branch-push run is therefore authoritative: it runs the
   # full build AND, when HEAD carries a release tag, the release/publish jobs.
   # `release_metadata` resolves that tag once so downstream jobs switch on
   # `is-release` and address the tag by name — `github.ref` is
   # `refs/heads/main` here, not the tag. A `workflow_dispatch` from a `v*` tag
   # ref (or from a tagged main HEAD) is also treated as a release.
   release_metadata:
      name: Resolve release metadata
      # Hosted, not omp-kata: this ~20 s git-tag probe gates the whole release
      # chain, and queueing it behind the kata pool adds dead minutes.
      runs-on: ubuntu-22.04
      outputs:
         is-release: ${{ steps.detect.outputs.is-release }}
         release-tag: ${{ steps.detect.outputs.release-tag }}
         # Canary tags publish to npm's canary dist-tag (derived script-side), mark
         # the GitHub release prerelease, and skip the stable-only brew tap.
         channel: ${{ steps.detect.outputs.channel }}
      steps:
         # Only a main-branch run needs tags fetched, so `git tag --points-at
         # HEAD` can see the freshly-pushed `v*`. A tag-ref dispatch reads the
         # tag straight from `github.ref_name`, and fetching `--tags` while
         # checkout uses an explicit tag refspec makes git refuse — so scope
         # fetch-tags to main refs.
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
           with:
              fetch-tags: ${{ github.ref == 'refs/heads/main' }}
         - name: Detect release tag at HEAD
           id: detect
           shell: bash
           run: |
              is_release=false
              release_tag=""
              channel=stable
              case "${{ github.ref }}" in
                refs/tags/v[0-9]*)
                  release_tag="${{ github.ref_name }}"
                  ;;
                refs/heads/main)
                  if [ "${{ github.event_name }}" != "pull_request" ]; then
                    release_tag=$(git tag --points-at HEAD | grep -E '^v[0-9]' | head -n1 || true)
                  fi
                  ;;
              esac
              if [ -n "$release_tag" ]; then
                echo "HEAD carries release tag $release_tag; this run builds and publishes the release."
                is_release=true
              fi
              if [[ "$release_tag" == *-canary.* ]]; then
                channel=canary
              fi
              {
                echo "is-release=$is_release"
                echo "release-tag=$release_tag"
                echo "channel=$channel"
              } >> "$GITHUB_OUTPUT"

   check:
      name: Lint, type check & web build
      runs-on: ubuntu-22.04
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: ./.github/actions/bun-install
         - name: Verify Rust license compliance (cargo-deny 0.20.2)
           run: |
              # Prebuilt release binary checked against digests pinned here:
              # `cargo install` compiled cargo-deny from source on every run
              # (~135 s), and a .sha256 fetched from the same mutable release
              # would prove transport integrity only. Bump ver and both
              # digests together.
              ver=0.20.2
              case "$(uname -m)" in
                x86_64) arch=x86_64 sha=9f12ed4c49936e09b48bf862b595cde2fe64fcbd9d74dfacac6131ca824c8d5f ;;
                aarch64|arm64) arch=aarch64 sha=995c82be0defc7a025cae49a2aa2644ce8245c9a3318fc4103907c6a285e8c7d ;;
                *) echo "::error::unsupported arch $(uname -m)"; exit 1 ;;
              esac
              name="cargo-deny-$ver-$arch-unknown-linux-musl"
              curl -fsSL --retry 3 -o "$RUNNER_TEMP/$name.tar.gz" "https://github.com/EmbarkStudios/cargo-deny/releases/download/$ver/$name.tar.gz"
              echo "$sha  $RUNNER_TEMP/$name.tar.gz" | sha256sum -c -
              tar -xzf "$RUNNER_TEMP/$name.tar.gz" -C "$RUNNER_TEMP"
              mkdir -p "$HOME/.cargo/bin"
              install -m 0755 "$RUNNER_TEMP/$name/cargo-deny" "$HOME/.cargo/bin/cargo-deny"
              # `--offline` needs every workspace crate already in the registry
              # cache. Every job runs on a fresh GitHub-hosted runner, where the
              # only prior cargo work is the cargo-deny install, so nothing
              # populates the index for our own graph and resolution fails.
              # Fetch first.
              cargo fetch --locked
              cargo deny --locked --offline check licenses sources
         - name: Type check workspace
           run: bun run ci:check:full
         - name: Brand residue guard
           run: bun scripts/brand/brand-check.ts
         - name: Zeta-only sentinel guard
           run: bun scripts/check-zeta-sentinels.ts
         - name: Build collab web
           run: bun run collab:web:build

   # Standalone web-ui build matrix (Appendix A §7 / simplify-and-plan-surface
   # §11): npm-only project — no bun-install, no native addons, no Rust. Catches
   # platform-specific breakage in the Next build that the single-Linux `check`
   # job cannot see. windows-2022 is deferred: `next build` hits an upstream
   # Next/webpack bug on the runner (EPERM scandir of the junctioned
   # 'C:\Users\...\Application Data' during compile, vercel/next.js#40760
   # family) that outputFileTracingRoot does not cover; re-add once upstream
   # fixes the compile-phase glob.
   web_ui_build:
      name: Web UI build (${{ matrix.os }})
      runs-on: ${{ matrix.os }}
      strategy:
         fail-fast: false
         matrix:
            os: [ubuntu-22.04, macos-14]
      defaults:
         run:
            working-directory: web-ui
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: actions/setup-node@v4
           with:
              node-version: 22
              cache: npm
              cache-dependency-path: web-ui/package-lock.json
         - name: Install dependencies
           run: npm ci
         - name: Type check
           run: npx tsc --noEmit
         - name: Lint
           run: npm run lint
         - name: Build
           run: npm run build

   # Rust validation (tests, clippy, rustfmt) runs on non-PR events only.
   # Native-changing PRs are rare enough that they do not warrant PR-side
   # Rust validation or a PR-side addon build (see native_addons): Rust is
   # validated post-merge on main (kata remote cache, warm) and again at
   # release. A skipped required check still satisfies branch protection.
   rust_validate:
      name: Validate Rust workspace (bazel)
      if: github.event_name != 'pull_request'
      runs-on: ubuntu-22.04
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: ./.github/actions/bun-install
         - id: cache
           uses: ./.github/actions/bazel-cache
           with:
              scope: linux
         - name: Rust tests
           # The ulimit guard runs in the step that launches the bazel server
           # (limits are per-process and the server persists across steps).
           run: |
              if [ "$(ulimit -Sn)" != unlimited ] && [ "$(ulimit -Sn)" -lt 65536 ]; then ulimit -Sn 65536 || true; fi
              bazelisk --bazelrc="${{ steps.cache.outputs.rc }}" test //crates/...
         # Clippy scope mirrors `cargo clippy --workspace` (libraries only, no
         # test targets) plus the strict/default split: crates with
         # `[lints] workspace = true` get the workspace policy, except
         # brush-core (a vendored fork excluded from the Cargo task too).
         # pi-builtins allows every clippy group in its own manifest (ported
         # brush/uutils/jaq code) but is still held to zero rustc warnings;
         # Cargo honors that via `[lints]`, Bazel via the clippy-ported config.
         - name: Clippy (workspace lint policy on opted-in crates)
           run: |
              bazelisk query "kind('rust_library|rust_shared_library', //crates/pi-ast/... + //crates/pi-diff/... + //crates/pi-edit/... + //crates/pi-iso/... + //crates/pi-natives/... + //crates/pi-shell/... + //crates/pi-vcs/... + //crates/pi-voice/... + //crates/pi-walker/...)" \
                | xargs bazelisk --bazelrc="${{ steps.cache.outputs.rc }}" build --config=clippy-strict --
         - name: Clippy (default lints elsewhere)
           run: |
              bazelisk query "kind('rust_library|rust_shared_library', //crates/... - (//crates/pi-ast/... + //crates/pi-diff/... + //crates/pi-edit/... + //crates/pi-iso/... + //crates/pi-natives/... + //crates/pi-shell/... + //crates/pi-vcs/... + //crates/pi-voice/... + //crates/pi-walker/...) - //crates/pi-builtins/... - //crates/vendor/brush-core/...)" \
                | xargs bazelisk --bazelrc="${{ steps.cache.outputs.rc }}" build --config=clippy --
         - name: Clippy (manifest allows on ported pi-builtins)
           run: |
              bazelisk query "kind('rust_library|rust_shared_library', //crates/pi-builtins/...)" \
                | xargs bazelisk --bazelrc="${{ steps.cache.outputs.rc }}" build --config=clippy-ported --
         - name: Rustfmt
           run: bazelisk --bazelrc="${{ steps.cache.outputs.rc }}" build --config=rustfmt //crates/...

   # Provides the native addons every downstream TS job installs. PRs never
   # build: they fetch the latest release's Linux x64 pair from the
   # @linxiraos/pi-natives-linux-x64 npm leaf instead. The workspace loader
   # skips its version sentinel for workspace loads, so release addons load
   # fine under a newer checkout; a PR whose TS tests depend on changed
   # native behavior fails visibly and the native side lands via main.
   # Main (kata, cluster remote cache) builds all Linux-hosted targets with
   # bazel. Job name says "bazel" for continuity with required checks.
   native_addons:
      name: Build native addons (bazel)
      runs-on: ubuntu-22.04
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

         # ---- PR path: latest Zeta addons from the @linxiraos npm leaves ----
         # Desktop PR jobs need the full target matrix, so fetch every leaf
         # (not just linux-x64) and unpack each into its bazel-bin layout.
         - name: Fetch release native addons (npm)
           if: github.event_name == 'pull_request'
           shell: bash
           run: |
              set -euo pipefail
              fetch_leaf() {
                 local pkg="$1" dest="$2" file="$3"
                 local tarball
                 tarball="$(npm view "@linxiraos/${pkg}@latest" dist.tarball)"
                 echo "Fetching ${tarball}"
                 rm -rf "$RUNNER_TEMP/leaf-package"
                 mkdir -p "$RUNNER_TEMP/leaf-package" "$dest"
                 curl -fsSL --retry 3 "$tarball" | tar -xz -C "$RUNNER_TEMP/leaf-package"
                 cp "$RUNNER_TEMP/leaf-package/package/${file}" "$dest/"
              }
              fetch_leaf pi-natives-linux-x64 bazel-bin/natives-linux-x64-baseline pi_natives.linux-x64-baseline.node
              fetch_leaf pi-natives-linux-x64 bazel-bin/natives-linux-x64-modern pi_natives.linux-x64-modern.node
              fetch_leaf pi-natives-win32-x64 bazel-bin/natives-win32-x64-baseline pi_natives.win32-x64-baseline.node
              # Only what the desktop PR jobs consume: linux-x64 (baseline +
              # modern) for the Linux desktop build and win32-x64-baseline for
              # the Windows one. musl/arm targets stay on the bazel path (no
              # musl leaf package exists).
         # A corrupt download must fail here, not as a confusing dlopen error
         # in every downstream test shard.
         - name: Smoke release addons
           if: github.event_name == 'pull_request'
           shell: bash
           run: |
              set -euo pipefail
              # NOTE: `bun -e 'require("./x.node")'` swallows dlopen failures
              # (exit 0 on a bogus addon); a script file enforces them in
              # both bun and node. Verified against a corrupt .node fixture.
              cat > "$RUNNER_TEMP/smoke-addons.js" <<'EOF'
              for (const f of process.argv.slice(2)) {
               const m = require(f);
               if (!m || Object.keys(m).length === 0) {
               	console.error(`addon failed to load: ${f}`);
               	process.exit(1);
               }
              }
              EOF
              loader=node
              if command -v bun >/dev/null 2>&1; then loader=bun; fi
              "$loader" "$RUNNER_TEMP/smoke-addons.js" \
                "$PWD/bazel-bin/natives-linux-x64-baseline/pi_natives.linux-x64-baseline.node" \
                "$PWD/bazel-bin/natives-linux-x64-modern/pi_natives.linux-x64-modern.node"
         # Make the policy visible on the rare native-touching PR instead of
         # leaving a reviewer to wonder which addons the tests exercised.
         # Best-effort: a diff-API failure must not fail the job over a notice.
         - name: Note native changes tested against release addons
           if: github.event_name == 'pull_request'
           shell: bash
           env:
              GH_TOKEN: ${{ github.token }}
           run: |
              if gh pr diff ${{ github.event.pull_request.number }} --name-only 2>/dev/null \
                 | grep -qE '^(crates/|bazel/|Cargo\.(toml|lock)|MODULE\.bazel(\.lock)?|BUILD\.bazel|\.bazelrc|\.bazelignore|\.bazelversion|rust-toolchain\.toml|rustfmt\.toml)'; then
                echo "::notice title=Native sources changed::PR CI tests against the latest release addons by design; native changes are validated post-merge on main and at release."
              fi

         # ---- main path: bazel build of all Linux-hosted targets ----
         - id: cache
           if: github.event_name != 'pull_request'
           uses: ./.github/actions/bazel-cache
           with:
              scope: linux
         - name: Build native addons once
           if: github.event_name != 'pull_request'
           run: |
              set -eo pipefail
              if [ "$(ulimit -Sn)" != unlimited ] && [ "$(ulimit -Sn)" -lt 65536 ]; then ulimit -Sn 65536 || true; fi
              # The addon cdylib links each peak at several GiB of rustc RSS;
              # the aggregate //:natives-linux-all build runs all six
              # concurrently, which OOMs the kata pod and takes the bazel
              # server with it (exit 37 "Server terminated abruptly", runs
              # 30556752623 / 30557524371). Build one addon per invocation —
              # analysis and cached actions are shared through the persistent
              # server, so only the heavy links serialize — then assemble the
              # aggregate as a no-op (also catches targets added to the
              # filegroup but missing from this list).
              : > "$RUNNER_TEMP/bazel-build.log"
              for target in \
                natives-linux-musl-x64-baseline \
                natives-linux-x64-baseline \
                natives-linux-x64-modern \
                natives-win32-x64-baseline; do
                bazelisk --bazelrc="${{ steps.cache.outputs.rc }}" build "//:$target" 2>&1 | tee -a "$RUNNER_TEMP/bazel-build.log"
              done
              bazelisk --bazelrc="${{ steps.cache.outputs.rc }}" build //:natives-linux-all 2>&1 | tee -a "$RUNNER_TEMP/bazel-build.log"
         # Cache-hit visibility: a supposedly warm build that executes
         # thousands of actions is the failure mode that made CI slow — make
         # it visible in the run summary instead of discovering it weeks in.
         - name: Report bazel cache stats
           if: github.event_name != 'pull_request'
           shell: bash
           run: |
              # Bazel runs with --color=yes (config=ci); strip ANSI before
              # matching — escapes split "INFO:" from the process count in
              # the raw log.
              summary=$(sed -E 's/\x1b\[[0-9;]*m//g' "$RUNNER_TEMP/bazel-build.log" | grep -E '[0-9]+ processes:' | tail -1 || true)
              echo "::notice title=Bazel build summary::${summary:-no process summary found}"

         # Split upload: every TS test job needs only the linux-x64 pair, but
         # downloading the full ~250 MB six-addon artifact cost ~100 s per job
         # on omp-kata. The native-artifacts action fetches `native-addons`
         # (everything else) only when a consumer asks for a non-x64 target.
         - name: Upload linux-x64 native addon artifact
           uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
           with:
              name: natives-linux-x64
              path: bazel-bin/natives-linux-x64-*/*.node
              if-no-files-found: error
              retention-days: 1
         - name: Upload remaining native addon artifacts
           if: github.event_name != 'pull_request'
           uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
           with:
              name: native-addons
              path: |
                 bazel-bin/natives-*/*.node
                 !bazel-bin/natives-linux-x64-*/*.node
              if-no-files-found: error
              retention-days: 1

   test_workspace:
      name: Test TS workspace fast
      runs-on: ubuntu-22.04
      needs: [native_addons]
      if: ${{ !cancelled() && needs.native_addons.result == 'success' }}
      timeout-minutes: 20
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: ./.github/actions/setup-system-deps
         - uses: ./.github/actions/bun-install
         - uses: ./.github/actions/native-artifacts
           with:
              targets: linux-x64-baseline linux-x64-modern
         - name: Test workspace packages and repo scripts (TS)
           env:
              OMP_TEST_CONCURRENCY: "4"
           run: |
              bun run ci:test:ts:workspace
              # Not `test:scripts`: scripts/musl-release.test.ts fails on main
              # (its install.sh smoke-check executes a fake binary), so running
              # the whole group here would red this job on an unrelated break.
              bun test scripts/ci-test-ts.test.ts scripts/release.test.ts

   test_coding_agent_singleton:
      name: Test coding-agent singleton/global-state (TS)
      runs-on: ubuntu-22.04
      needs: [native_addons]
      if: ${{ !cancelled() && needs.native_addons.result == 'success' }}
      timeout-minutes: 20
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: ./.github/actions/setup-system-deps
         - uses: ./.github/actions/bun-install
         - uses: ./.github/actions/native-artifacts
           with:
              targets: linux-x64-baseline linux-x64-modern
         - name: Test coding-agent singleton/global-state bucket
           # Keep global Settings/env/fake-timer tests serial; native addon
           # artifacts are still available like every other coding-agent bucket.
           run: bun run ci:test:coding-agent:singleton

   test_ts_native:
      name: Test TS native/integration packages
      runs-on: ubuntu-22.04
      needs: [native_addons]
      if: ${{ !cancelled() && needs.native_addons.result == 'success' }}
      timeout-minutes: 25
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: ./.github/actions/setup-system-deps
         - uses: ./.github/actions/bun-install
         - uses: ./.github/actions/native-artifacts
           with:
              targets: linux-x64-baseline linux-x64-modern
         - name: Test native/TUI/browser-ish packages (TS)
           env:
              OMP_TEST_CONCURRENCY: "4"
           run: bun run ci:test:ts:native

   test_coding_agent_ui:
      name: Test coding-agent UI/TUI (TS)
      runs-on: ubuntu-22.04
      needs: [native_addons]
      if: ${{ !cancelled() && needs.native_addons.result == 'success' }}
      timeout-minutes: 25
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: ./.github/actions/setup-system-deps
         - uses: ./.github/actions/bun-install
         - uses: ./.github/actions/native-artifacts
           with:
              targets: linux-x64-baseline linux-x64-modern
         - name: Test coding-agent UI/TUI bucket
           env:
              OMP_TEST_CONCURRENCY: "2"
           run: bun run ci:test:coding-agent:ui

   test_coding_agent_runtime:
      name: Test coding-agent runtime/session (TS)
      runs-on: ubuntu-22.04
      needs: [native_addons]
      if: ${{ !cancelled() && needs.native_addons.result == 'success' }}
      timeout-minutes: 25
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: ./.github/actions/setup-system-deps
         - uses: ./.github/actions/bun-install
         - uses: ./.github/actions/native-artifacts
           with:
              targets: linux-x64-baseline linux-x64-modern
         - name: Test coding-agent runtime bucket
           # Runtime/session tests import native-backed barrels too; keep this
           # separate for concurrency, not as a native-free guardrail.
           env:
              OMP_TEST_CONCURRENCY: "4"
           run: bun run ci:test:coding-agent:runtime

   test_coding_agent_native:
      name: Test coding-agent native/unit (TS)
      runs-on: ubuntu-22.04
      needs: [native_addons]
      if: ${{ !cancelled() && needs.native_addons.result == 'success' }}
      timeout-minutes: 25
      # The longest PR job, so it runs as two shards on hosted PR runners. Main
      # keeps one: omp-kata caps at 8 runners and the fan-out already fills it.
      # No required check keys on this job name (branch protection and
      # rulesets are off for main/omp2), so the per-shard names are safe.
      strategy:
         fail-fast: false
         matrix:
            shard: ${{ fromJSON(github.event_name == 'pull_request' && '["1/2","2/2"]' || '["1/1"]') }}
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: ./.github/actions/setup-system-deps
         - uses: ./.github/actions/bun-install
         - uses: ./.github/actions/native-artifacts
           with:
              targets: linux-x64-baseline linux-x64-modern
         - name: Test coding-agent native/unit bucket
           env:
              OMP_TEST_CONCURRENCY: "4"
              # Hosted runners resolve system Chrome but CDP never comes up in the
              # real-browser pickElectronTarget trio (upstream ubuntu-22.04 PR runs
              # fail identically; upstream gates on self-hosted omp-kata). Zeta has
              # no self-hosted runner, so the trio self-skips here; local runs keep
              # full coverage.
              ZETA_SKIP_REAL_BROWSER: "1"
           run: bun run ci:test:coding-agent:native

   test_smoke:
      name: Test CLI smoke (TS)
      runs-on: ubuntu-22.04
      needs: [native_addons]
      if: ${{ !cancelled() && needs.native_addons.result == 'success' }}
      timeout-minutes: 15
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: ./.github/actions/setup-system-deps
         - uses: ./.github/actions/bun-install
         - uses: ./.github/actions/native-artifacts
           with:
              targets: linux-x64-baseline linux-x64-modern
         - name: CLI smoke test
           run: bun run ci:test:smoke

   install_methods:
      name: Install method smoke tests
      runs-on: ubuntu-22.04
      needs: [native_addons]
      if: ${{ !cancelled() && needs.native_addons.result == 'success' }}
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: ./.github/actions/setup-system-deps
         - uses: ./.github/actions/bun-install
         - uses: ./.github/actions/native-artifacts
           with:
              targets: linux-x64-baseline linux-x64-modern
         - name: Install method smoke tests
           env:
              OMP_INSTALL_TEST_SKIP_NATIVE_BUILD: "1"
           run: bun run ci:test:install-methods

   # Desktop shells (Electron embedding the standalone web-ui + a compiled
   # zeta runtime). They run on every CI event like the test fan-out: PR-side
   # they prove the pipeline compiles and smokes; at release the artifacts
   # feed release_github as installer assets. Linux/Windows reuse the
   # native_addons artifact; macOS builds its own addon (no cross-hosted
   desktop_linux:
      name: Build and smoke desktop (Linux)
      runs-on: ubuntu-22.04
      needs: [release_metadata, native_addons]
      # Release-only: the PR path fetches npm-leaf natives (@latest), whose
      # version lags the workspace line between releases and fails the
      # desktop embed sentinel check. Release runs build their own addons,
      # so the sentinel holds there.
      if: ${{ needs.release_metadata.outputs.is-release == 'true' && !cancelled() && needs.native_addons.result == 'success' }}
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
           with:
              node-version: "24"
         - uses: ./.github/actions/bun-install
         - uses: ./.github/actions/native-artifacts
           with:
              targets: linux-x64-baseline linux-x64-modern
         - name: Install Web UI dependencies
           working-directory: web-ui
           run: npm ci
         - name: Install desktop dependencies
           working-directory: desktop
           run: npm ci
         - name: Test desktop platform contracts
           working-directory: desktop
           run: npm test
         - name: Build Linux desktop artifact
           working-directory: desktop
           run: npm run dist
         - name: Guard smoke against lagging npm natives
           if: github.event_name == 'pull_request'
           id: natives_guard
           shell: bash
           run: |
              set -euo pipefail
              # The release line's authoritative source is packages/utils/package.json
              # (same file check-version-consistency.ts reads); the root manifest is a
              # private workspace and carries no version field.
              workspace_version="$(node -p "require('./packages/utils/package.json').version")"
              published_version="$(npm view @linxiraos/pi-natives-linux-x64 version)"
              if [ "$published_version" != "$workspace_version" ]; then
                 echo "::warning::npm natives leaf (${published_version}) lags the workspace release line (${workspace_version}); the packaged runtime would fail the loader sentinel check. PR desktop smoke is skipped — push/tag runs build addons from source and stay fully gated."
              else
                 echo "run_smoke=1" >> "$GITHUB_OUTPUT"
              fi
         - name: Smoke Linux desktop artifact
           if: github.event_name != 'pull_request' || steps.natives_guard.outputs.run_smoke == '1'
           run: |
              set -euo pipefail
              app_dir="$(find temp/desktop/release -mindepth 1 -maxdepth 1 -type d -name 'zeta-desktop-*-linux-x64' -print -quit)"
              test -n "$app_dir"
              npm --prefix desktop run smoke -- --app "$(realpath "$app_dir")"
         - name: Upload Linux desktop artifacts
           uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
           with:
              name: zeta-desktop-linux
              path: |
                 temp/desktop/release/*.tar.gz
                 temp/desktop/release/*.deb
                 temp/desktop/release/*.AppImage
              if-no-files-found: error
              retention-days: 1

   desktop_windows:
      name: Build and smoke desktop (Windows)
      runs-on: windows-2022
      needs: [release_metadata, native_addons]
      # Release-only: same reason as desktop_linux (see above).
      if: ${{ needs.release_metadata.outputs.is-release == 'true' && !cancelled() && needs.native_addons.result == 'success' }}
      timeout-minutes: 40
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
           with:
              node-version: "24"
         - uses: ./.github/actions/bun-install
         - uses: ./.github/actions/native-artifacts
           with:
              targets: win32-x64-baseline
         - name: Verify Bun is available to Windows desktop build
           shell: pwsh
           run: bun --version
         - name: Install Web UI dependencies
           working-directory: web-ui
           run: npm ci
         - name: Install desktop dependencies
           working-directory: desktop
           run: npm ci
         - name: Test desktop platform contracts
           working-directory: desktop
           run: npm test
         - name: Build Windows desktop artifact
           working-directory: desktop
           # The runner's junctioned profile dirs (C:\Users\runneradmin\
           # Application Data -> AppData\Roaming) raise EPERM when webpack
           # walks them during next build. Point the profile vars at empty
           # dirs so nothing traces through the junctions.
           env:
              APPDATA: ${{ runner.temp }}/desktop-profile
              USERPROFILE: ${{ runner.temp }}/desktop-profile
              HOME: ${{ runner.temp }}/desktop-profile
           run: |
              New-Item -ItemType Directory -Force "$env:APPDATA" | Out-Null
              npm run dist
         - name: Guard smoke against lagging npm natives
           if: github.event_name == 'pull_request'
           id: natives_guard
           shell: bash
           run: |
              set -euo pipefail
              # The release line's authoritative source is packages/utils/package.json
              # (same file check-version-consistency.ts reads); the root manifest is a
              # private workspace and carries no version field.
              workspace_version="$(node -p "require('./packages/utils/package.json').version")"
              published_version="$(npm view @linxiraos/pi-natives-win32-x64 version)"
              if [ "$published_version" != "$workspace_version" ]; then
                 echo "::warning::npm natives leaf (${published_version}) lags the workspace release line (${workspace_version}); the packaged runtime would fail the loader sentinel check. PR desktop smoke is skipped — push/tag runs build addons from source and stay fully gated."
              else
                 echo "run_smoke=1" >> "$GITHUB_OUTPUT"
              fi
         - name: Smoke Windows desktop artifact
           if: github.event_name != 'pull_request' || steps.natives_guard.outputs.run_smoke == '1'
           shell: pwsh
           run: |
              $app = Get-ChildItem temp/desktop/release -Directory | Where-Object Name -Like "zeta-desktop-*-win-x64" | Select-Object -First 1
              if ($null -eq $app) { throw "Windows desktop artifact not found" }
              npm --prefix desktop run smoke -- --app $app.FullName
         - name: Upload Windows desktop artifacts
           uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
           with:
              name: zeta-desktop-win
              path: |
                 temp/desktop/release/*.zip
                 temp/desktop/release/*.exe
              if-no-files-found: error
              retention-days: 1

   # Aggregates every validation job so publish-side release jobs gate on one
   # result. Binary BUILDS deliberately do not wait for this gate — they run
   # in parallel with the test fan-out and only publishing is held back.
   release_gate:
      name: Release validation gate
      if: ${{ needs.release_metadata.outputs.is-release == 'true' && !cancelled() &&
         needs.rust_validate.result == 'success' &&
         needs.native_addons.result == 'success' &&
         needs.test_workspace.result == 'success' &&
         needs.test_coding_agent_singleton.result == 'success' &&
         needs.test_ts_native.result == 'success' &&
         needs.test_coding_agent_ui.result == 'success' &&
         needs.test_coding_agent_runtime.result == 'success' &&
         needs.test_coding_agent_native.result == 'success' &&
         needs.test_smoke.result == 'success' && needs.check.result == 'success' &&
         needs.install_methods.result == 'success' }}
      needs: [release_metadata, check, rust_validate, native_addons, test_workspace, test_coding_agent_singleton, test_ts_native, test_coding_agent_ui, test_coding_agent_runtime, test_coding_agent_native, test_smoke, install_methods]
      runs-on: ubuntu-22.04
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: ./.github/actions/bun-install
         # Generated-config drift gate: bazel/clippy.bazelrc is maintained by
         # scripts/release.ts (like the lockfiles); a stale file only blocks
         # publishing, never ordinary CI.
         - name: Check generated clippy config
           run: bun scripts/gen-clippy-bazelrc.ts --check
         - run: echo "release validation green"

   # Builds (does not publish) the Linux-hosted release binaries in parallel
   # with the test fan-out; the single need is native_addons, whose artifact
   # supplies their addons.
   # and start at release_metadata time instead. Publishing (npm leaves,
   # GitHub release, core npm) is gated on release_gate downstream.
   release_binary:
      name: "Release binary: ${{ matrix.target_id }}"
      if: ${{ needs.release_metadata.outputs.is-release == 'true' && !cancelled() &&
         needs.native_addons.result == 'success' }}
      needs: [release_metadata, native_addons]
      strategy:
         fail-fast: false
         matrix:
            include:
               - {
                    os: ubuntu-22.04,
                    platform: linux,
                    arch: x64,
                    target_id: linux-x64,
                    binary_path: packages/coding-agent/binaries/zeta-cli-linux-x64,
                    native_targets: linux-x64-baseline linux-x64-modern,
                 }
               - {
                    os: ubuntu-22.04,
                    platform: linux,
                    libc: musl,
                    arch: x64,
                    target_id: linux-musl-x64,
                    binary_path: packages/coding-agent/binaries/zeta-cli-linux-musl-x64,
                    native_targets: linux-musl-x64-baseline,
                 }
               - {
                    os: ubuntu-22.04,
                    platform: win32,
                    arch: x64,
                    target_id: win32-x64,
                    binary_path: packages/coding-agent/binaries/zeta-cli-windows-x64.exe,
                    native_targets: win32-x64-baseline,
                 }
      runs-on: ${{ matrix.os }}
      permissions:
         contents: read
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
         - name: Restore bun store
           id: bun-cache
           uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
           with:
              path: ~/.bun/install/cache
              key: bun-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
              restore-keys: |
                 bun-${{ runner.os }}-
         - run: bun install --frozen-lockfile
         # Main-only save: caches saved on a tag or branch are readable only by
         # that ref yet count against the shared 10 GB quota (see bazel-cache).
         - name: Save bun store
           if: github.ref == 'refs/heads/main' && steps.bun-cache.outputs.cache-hit != 'true'
           uses: actions/cache/save@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
           with:
              path: ~/.bun/install/cache
              key: bun-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
         - name: Install prebuilt native addon(s)
           uses: ./.github/actions/native-artifacts
           with:
              targets: ${{ matrix.native_targets }}
         - name: Build release binary
           env:
              RELEASE_TARGETS: ${{ matrix.target_id }}
           run: bun run ci:release:build-binaries
         # Windows binary is cross-built on Linux, so we have no Windows runner
         # to smoke it on. Cross-build correctness is verified via the bun
         # `--compile --target=bun-windows-x64-*` cross-compile. Musl binaries
         # need the musl loader, which glibc runners lack — they are smoked in
         # the Alpine container step below instead.
         - name: Smoke release binary
           if: matrix.platform != 'win32' && matrix.libc != 'musl'
           run: |
              runtime_dir="$(mktemp -d)"
              HOME="$runtime_dir/home" XDG_DATA_HOME="$runtime_dir/xdg" "${{ matrix.binary_path }}" --version
              HOME="$runtime_dir/home" XDG_DATA_HOME="$runtime_dir/xdg" "${{ matrix.binary_path }}" --smoke-test
         - name: Smoke musl release binary on Alpine
           if: matrix.libc == 'musl'
           run: |
              binary="$(realpath "${{ matrix.binary_path }}")"
              # Bun's musl-target binaries link libstdc++/libgcc dynamically;
              # Alpine users install them alongside the binary (same as bun itself).
              docker run --rm -v "$binary:/usr/local/bin/omp:ro" alpine:3.22 sh -ec '
                apk add --no-cache libstdc++ libgcc >/dev/null
                runtime_dir="$(mktemp -d)"
                HOME="$runtime_dir/home" XDG_DATA_HOME="$runtime_dir/xdg" omp --version
                HOME="$runtime_dir/home" XDG_DATA_HOME="$runtime_dir/xdg" omp --smoke-test
              '
         - name: Upload release binary artifact
           uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
           with:
              name: zeta-binary-${{ matrix.target_id }}
              path: ${{ matrix.binary_path }}

   # Native-host builds make addons that native_addons cannot provide: Darwin
   release_native_leaves:
      name: Publish native leaf packages
      if: ${{ needs.release_metadata.outputs.is-release == 'true' && !cancelled() &&
         needs.release_gate.result == 'success' &&
         needs.release_binary.result == 'success' &&
         needs.desktop_linux.result == 'success' &&
         needs.desktop_windows.result == 'success' &&
         !inputs.skip_npm }}
      needs: [release_metadata, release_gate, release_binary, desktop_linux, desktop_windows]
      runs-on: ubuntu-22.04
      # id-token lets npm mint the OIDC token for trusted publishing; the
      # NODE_AUTH_TOKEN below is the fallback for unconfigured packages.
      permissions:
         id-token: write
         contents: read
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
         - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
           with:
              node-version: "24"
              registry-url: "https://registry.npmjs.org"
         # npm runs under Bun when invoked by the release script; npm 12
         # requires a newer emulated Node version than Bun 1.3 provides.
         - name: Ensure npm supports trusted publishing
           run: npm install -g npm@11.17.0
         - name: Restore bun store
           id: bun-cache
           uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
           with:
              path: ~/.bun/install/cache
              key: bun-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
              restore-keys: |
                 bun-${{ runner.os }}-
         - run: bun install --frozen-lockfile
         # Main-only save: caches saved on a tag or branch are readable only by
         # that ref yet count against the shared 10 GB quota (see bazel-cache).
         - name: Save bun store
           if: github.ref == 'refs/heads/main' && steps.bun-cache.outputs.cache-hit != 'true'
           uses: actions/cache/save@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
           with:
              path: ~/.bun/install/cache
              key: bun-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
         # Linux + win32-x64 addons from the native_addons artifact (gnu targets
         # only — musl reuses the linux filenames and has no leaf package).
         - name: Install linux and win32-x64 native addons
           uses: ./.github/actions/native-artifacts
           with:
              targets: linux-x64-baseline linux-x64-modern win32-x64-baseline
         # Native-host addons come flat (canonical filenames) from the
         - name: Install native-host addons
           uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
           with:
              pattern: native-addons-*
              path: packages/natives/native
              merge-multiple: true
         - name: Publish native leaf packages
           env:
              # Fallback auth: setup-node wrote an .npmrc referencing
              # NODE_AUTH_TOKEN; npm uses it only when OIDC has no trusted
              # publisher for the package (or on a first publish).
              NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
           run: |
              set -euo pipefail
              for tag in linux-x64 win32-x64; do
                bun run ci:release:publish-native-leaf "$tag"
              done


   # Vendored editor (TTT) upstream test suites, run on the same Linux
   # environment the upstream project targets. Gates every PR/push that
   # touches the vendor tree so the snapshot never silently rots: Go unit +
   # e2e tests (chaos stays behind its build tag) and the functional vitest
   # suite. Integration tests need downloadable LSP servers and are not part
   # of the gate.
   editor_tests:
      name: Test editor (vendored TTT)
      runs-on: ubuntu-22.04
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
           with:
              go-version-file: editor/go.mod
              cache-dependency-path: editor/go.sum
         - name: Go tests (unit + e2e)
           working-directory: editor
           run: go test ./...
         - name: Build editor binary for functional harness
           working-directory: editor
           run: go build -ldflags="-s -w -X main.version=ci-test" -o bin/ttt ./cmd/ttt
         - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
           with:
              node-version: "24"
         - name: Functional vitest suite
           working-directory: editor/tests/functional
           run: npm install --no-audit --no-fund && npx vitest run

   # Publishes the vendored editor npm distribution (@linxiraos/editor plus
   # its windows/linux x64 platform leaves) on release tags via npm trusted
   # publishing. Binaries are prebuilt and committed under editor/npm/*/bin,
   # so no cross-platform build legs are needed. Does not gate the GitHub
   # release itself (same policy as native leaves).
   release_editor_packages:
      name: Publish editor npm packages
      if: ${{ needs.release_metadata.outputs.is-release == 'true' && !cancelled() &&
         needs.release_gate.result == 'success' &&
         !inputs.skip_npm }}
      # npm Trusted Publisher binds the workflow file that EXECUTES the
      # publish. A `uses:` call keeps editor-publish.yml as the callee, but
      # the OIDC claim still names the caller (ci.yml) — v1.1.19's E404. So
      # dispatch the dedicated workflow instead and wait for it: the publish
      # truly runs inside editor-publish.yml, matching the binding, and the
      # same file keeps serving manual republishes via `gh workflow run`.
      needs: [release_metadata, release_gate]
      runs-on: ubuntu-22.04
      permissions:
         contents: read
         actions: write
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
         - name: Dispatch editor-publish.yml and wait for it
           env:
              GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
           run: |
              set -euo pipefail
              gh workflow run editor-publish.yml --ref "${GITHUB_REF_NAME}"
              rid=""
              for _ in $(seq 1 30); do
                 sleep 10
                 rid=$(gh run list --workflow=editor-publish.yml --branch "${GITHUB_REF_NAME}" --limit 1 --json databaseId --jq '.[0].databaseId' 2>/dev/null || true)
                 [ -n "$rid" ] && break
              done
              if [ -z "$rid" ]; then
                 echo "::error::editor-publish run never appeared"
                 exit 1
              fi
              gh run watch "$rid" --exit-status --interval 15

   release_github:
      name: Publish GitHub release
      if: ${{ needs.release_metadata.outputs.is-release == 'true' && !cancelled() &&
         needs.release_gate.result == 'success' &&
         needs.release_binary.result == 'success' &&
         needs.desktop_linux.result == 'success' &&
         needs.desktop_windows.result == 'success' &&
         !inputs.skip_npm }}
      # v1.1.18 damage class 10 recurrence: without the desktop deps this job
      # started while desktop builds were still running, found 0
      # zeta-desktop-* installers, and the validation aborted the release.
      needs: [release_metadata, release_gate, release_binary, desktop_linux, desktop_windows]
      runs-on: ubuntu-22.04
      permissions:
         contents: write
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
         - name: Generate release notes from CHANGELOGs
           env:
              # `gh release list` (used to find the latest published GitHub
              # Release tag below the target so silent-tag changelog sections
              # roll forward — #2596) requires GH_TOKEN in Actions. Without
              # it gh exits non-zero and the script would degrade to legacy
              # single-version notes, defeating the recovery.
              GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
           run: bun scripts/ci-release-notes.ts ${{ needs.release_metadata.outputs.release-tag }}
         - name: Download release binaries
           uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
           with:
              pattern: zeta-binary-*
              path: packages/coding-agent/binaries
              merge-multiple: true
         # Desktop installers/archives from the three desktop jobs.
         - name: Download desktop artifacts
           uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
           with:
              pattern: zeta-desktop-*
              path: temp/desktop/release
              merge-multiple: true
         # Dependency-free build (bun APIs + zip only), so no `bun install`
         # is needed on this runner.
         - name: Build browser relay artifacts
           run: bun run --cwd packages/browser-relay build
         # Preflight: every release asset must exist before checksums hash it.
         # A stale asset-name reference (e.g. the v1.1.9 run where ci.yml still
         # pointed at omp-browser-relay-extension.zip after the build script
         # renamed it) fails here with a clear list instead of mid-checksums.
         - name: Preflight release assets
           run: |
              missing=0
              for asset in \
                packages/browser-relay/dist/zeta-browser-relay-extension.zip \
                LICENSE \
                THIRD-PARTY-NOTICES.txt; do
                if [ ! -f "$asset" ]; then
                  echo "::error::Missing release asset: $asset"
                  missing=1
                fi
              done
              count=$(ls packages/coding-agent/binaries/zeta-cli-* 2>/dev/null | wc -l)
              if [ "$count" -lt 3 ]; then
                # Release matrix is trimmed to linux-x64, linux-musl-x64 and
                # win32-x64 while macOS/ARM desktop publishing is paused.
                echo "::error::Expected at least 3 zeta-cli-* binaries, found $count"
                missing=1
              fi
              desktop_count=$(find temp/desktop/release -type f \( -name 'zeta-desktop-*.zip' -o -name 'zeta-desktop-*.exe' -o -name 'zeta-desktop-*.deb' -o -name 'zeta-desktop-*.AppImage' -o -name 'zeta-desktop-*.tar.gz' \) 2>/dev/null | wc -l)
              if [ "$desktop_count" -lt 5 ]; then
                # win32 zip+nsis-exe and linux tar.gz+deb+AppImage; macOS is
                # still paused (document/release.md).
                echo "::error::Expected at least 5 zeta-desktop-* installers/archives, found $desktop_count"
                missing=1
              fi
              exit $missing
         # Generated after every other release asset is in place and before
         # the release is created, so SHA256SUMS.txt itself ships as an asset
         # and covers every other file uploaded alongside it.
         - name: Generate checksums
           run: |
              bun run ci:release:checksums SHA256SUMS.txt \
                packages/coding-agent/binaries/zeta-cli-* \
                packages/browser-relay/dist/zeta-browser-relay-extension.zip \
                temp/desktop/release/zeta-desktop-* \
                LICENSE \
                THIRD-PARTY-NOTICES.txt
         - name: Create GitHub Release
           uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2.6.2
           with:
              tag_name: ${{ needs.release_metadata.outputs.release-tag }}
              prerelease: ${{ needs.release_metadata.outputs.channel == 'canary' }}
              files: |
                 packages/coding-agent/binaries/zeta-cli-*
                 packages/browser-relay/dist/zeta-browser-relay-extension.zip
                 temp/desktop/release/zeta-desktop-*
                 LICENSE
                 THIRD-PARTY-NOTICES.txt
                 SHA256SUMS.txt

   release_npm:
      name: Publish to npm
      if: ${{ needs.release_metadata.outputs.is-release == 'true' && !cancelled() &&
         needs.release_binary.result == 'success' &&
         needs.release_native_leaves.result == 'success' &&
         !inputs.skip_npm }}
      # release_native_leaves must finish first: the core package lists the
      # leaf packages as lockstep-versioned optionalDependencies, so installs
      # resolve them the moment the core version is live.
      needs: [release_metadata, release_binary, release_native_leaves]
      runs-on: ubuntu-22.04
      # `id-token: write` lets npm mint the GitHub OIDC token it exchanges for a
      # short-lived publish token (trusted publishing + provenance). When a
      # package has no matching trusted publisher configured, npm silently falls
      # back to NODE_AUTH_TOKEN below — which also covers first-ever publishes.
      permissions:
         id-token: write
         contents: read
      steps:
         - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
         - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
         - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
           with:
              node-version: "24"
              registry-url: "https://registry.npmjs.org"
         # npm runs under Bun when invoked by the release script; npm 12
         # requires a newer emulated Node version than Bun 1.3 provides.
         - name: Ensure npm supports trusted publishing
           run: npm install -g npm@11.17.0
         - name: Restore bun store
           id: bun-cache
           uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
           with:
              path: ~/.bun/install/cache
              key: bun-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
              restore-keys: |
                 bun-${{ runner.os }}-
         - run: bun install --frozen-lockfile
         # Main-only save: caches saved on a tag or branch are readable only by
         # that ref yet count against the shared 10 GB quota (see bazel-cache).
         - name: Save bun store
           if: github.ref == 'refs/heads/main' && steps.bun-cache.outputs.cache-hit != 'true'
           uses: actions/cache/save@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
           with:
              path: ~/.bun/install/cache
              key: bun-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
         # The prepack executes workspace code which loads the Linux x64
         # addon, so install the native_addons job's artifact before publishing.
         - name: Install prebuilt native addons
           uses: ./.github/actions/native-artifacts
           with:
              targets: linux-x64-baseline linux-x64-modern
         - name: Publish to npm
           env:
              # Fallback auth: setup-node wrote an .npmrc referencing
              # NODE_AUTH_TOKEN; npm uses it only when OIDC has no trusted
              # publisher for the package (or on a first publish).
              NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
           run: bun run ci:release:publish

